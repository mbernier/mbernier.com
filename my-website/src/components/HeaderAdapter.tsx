'use client';

import Header from './Header';

export default function HeaderAdapter() {
  // For now, just use the new Header component directly
  // This adapter is here for backwards compatibility
  return <Header />;
} 